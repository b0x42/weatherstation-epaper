# Pirate Weather API (required)
PIRATE_WEATHER_API_KEY=your_api_key_here

# Location
LATITUDE=52.5200
LONGITUDE=13.4050

# Language for weather summaries (see https://pirateweather.net/en/latest/API/#language)
LANGUAGE=de

# Units: "si" for Celsius, "us" for Fahrenheit
UNITS=si

# Display orientation: set to "true" if mounted upside down
FLIP_DISPLAY=false

# Update interval in seconds (1800 = 30 minutes)
UPDATE_INTERVAL_SECONDS=1800

# Log file path (optional)
# Default: /var/log/weatherstation.log (requires write permissions)
# For development on macOS, you can use a local path:
# LOG_FILE_PATH=./weatherstation.log
# LOG_FILE_PATH=/tmp/weatherstation.log

# Display Model Selection (104x212 resolution displays)
# Supported models:
#   - epd2in13bc (default) - Bi-color (black/red)
#   - epd2in13d - Monochrome (black/white) with partial update support
DISPLAY_MODEL=epd2in13bc

# Emulator Mode (for development/testing without hardware)
# Set to "true" to use EPD-Emulator instead of physical display
# Requires: https://github.com/benjaminburzan/EPD-Emulator
# USE_EMULATOR=true
USE_EMULATOR=false

# Font Path (optional, for macOS development)
# Default: /usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf (pre-installed on Raspberry Pi OS)
# For macOS development, download font and set path:
# FONT_PATH=~/Library/Fonts/DejaVuSans.ttf
# See Development & Testing section in README.md
